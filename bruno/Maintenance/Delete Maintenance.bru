meta {
  name: Delete Maintenance
  type: http
  seq: 8
}

delete {
  url: {{BASE_URL}}/maintenance/:id
  body: none
  auth: inherit
}

params:path {
  id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
